#include <bits/stdc++.h> // Allows us to include entire standart library. e.g iostream, vector
using namespace std;

int main() {
  // Solution comes here
  // Compile the code with the following:
  // g++ -std=c++11 -o2 -Wall Template.cc -o template
}
