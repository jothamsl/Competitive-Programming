#include <stdio.h>

// You are given all numbers between 1,2,…,n except one.
// Your task is to find the missing number.

int main(void) {
  int n;
  scanf("%d", &n);
  char values[n];
  printf("[Amount] -> %d\n [Numbers] -> %s\n", n, values);
  return 0;
}
